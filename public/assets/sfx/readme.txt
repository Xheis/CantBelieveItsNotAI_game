Optional SFX folder.

The current implementation includes a lightweight WebAudio synth fallback, so this folder is not required to run.

If you want to replace synthesized sounds with recorded assets later, add files here and update public/js/audio.js to load them.